# VSCode / Cursor editor config (placeholder)

Reserved for shared editor configuration, e.g.:

- `settings.json` (user settings)
- `keybindings.json`
- snippets

Not yet wired into the installers. Add files here, then extend the mapping
(target: the editor's user config dir, which differs per OS).
